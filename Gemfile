# frozen_string_literal: true

# Specify the source for gems
source "https://rubygems.org"

# Specify the Jekyll version
gem "jekyll", "~> 4.3" # Or your preferred Jekyll version

# Specify the theme (Minima is the default but good to list)
gem "minima", "~> 2.5"

# Add other plugins you might use (referenced in _config.yml)
gem "jekyll-feed", "~> 0.17.0"
gem "jekyll-seo-tag", "~> 2.8.0"

# Required by `jekyll serve` on Ruby 3.0+
# gem "webrick", "~> 1.8" # Uncomment if you run locally on Ruby 3+

# If deploying to GitHub Pages, uncomment the following line
# gem "github-pages", group: :jekyll_plugins

# Windows does not include zoneinfo files, so bundle the tzinfo-data gem
# gem "tzinfo-data", platforms: [:mingw, :x64_mingw, :mswin]
